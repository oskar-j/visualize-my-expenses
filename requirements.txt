# Runtime dependencies. Prefer `pip install vme-py` -- or, in a clone, `uv sync`
# or `pip install -e .` -- which read pyproject.toml; this file is kept for
# convenience / older tooling.
click>=8.0
matplotlib>=3.5

# Optional extras:
#   pip install "vme-py[excel]"  -> openpyxl, for .xlsx statements
#   pip install "vme-py[html]"   -> plotly, for interactive HTML output
